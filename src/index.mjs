import m from 'mithril';

// import pages

import { setup } from '@/helper/mock/browser';
import './global.scss'

// add mithril routes as needed....

// Setup the mode service worker if it is enabled in Globals
setup();